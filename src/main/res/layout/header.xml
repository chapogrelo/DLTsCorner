<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="178dp"
    android:background="@color/ColorPrimary"
    android:layout_marginBottom="@dimen/drawer_vertical_padding"
    android:paddingBottom="@dimen/drawer_vertical_padding"
    >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:paddingLeft="@dimen/drawer_horizontal_padding"
        android:paddingStart="@dimen/drawer_horizontal_padding">

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#ffffff"
            tools:text="Chapogrelo"
            android:textSize="14sp"
            android:textStyle="bold"
            />

        <TextView
            android:id="@+id/mojo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#ffffff"
            android:layout_marginTop="5dp"
            tools:text="Vers l'infini et au dela"
            android:textSize="14sp"
            android:textStyle="normal"/>
    </LinearLayout>

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@drawable/darkvatar"
        android:layout_marginLeft="@dimen/drawer_horizontal_padding"
        android:layout_marginTop="38dp"
        android:id="@+id/circleView"
        />
</RelativeLayout>